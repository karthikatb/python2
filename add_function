#include<stdio.h>
int add(int,int);
int main()
{
int x,y,result;
result=add(x,y);
printf("sum=%d",result);
return 0;
}
int add(int a,int b)
{
    int sum=0;
    printf("enter the 2 numbers:\n");
    scanf("%d%d",&a,&b);
    sum=a+b;
    return sum;
}
